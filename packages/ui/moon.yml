project:
  name: ui

dependsOn:
  - engine

language: "typescript"

tasks:
  build:
    command: "bunx rslib"
    inputs:
      - "src/**/*"
    outputs:
      - "dist"
  dev:
    command: "bunx rslib -w --env-mode dev"
    inputs:
      - "src/**/*"
