{
  "name": "@falkura-pet/mono",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build_all": "turbo run build",
    "dev_all": "turbo run dev",
    "build": "turbo run build --filter='./packages/*' --filter='./games/template'",
    "dev": "turbo run dev --filter='./packages/*' --filter='./games/template'",
    "serve": "serve dist -p 3001"
  },
  "dependencies": {
    "pixi.js": "^8.13.2"
  },
  "devDependencies": {
    "@assetpack/core": "^1.5.0",
    "@rsbuild/core": "^1.7.2",
    "@rslib/core": "^0.19.1",
    "@types/bun": "^1.3.5",
    "concurrently": "^9.2.1",
    "serve": "^14.2.5",
    "turbo": "^2.6.1",
    "typescript": "^5.9.3"
  },
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "packages/*",
    "games/*"
  ]
}
